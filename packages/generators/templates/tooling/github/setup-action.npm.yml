name: "Setup & Install"
description: "Setup & Install"

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version-file: ".nvmrc"
        cache: "npm"

    - name: Install Dependencies
      shell: bash
      run: npm ci
